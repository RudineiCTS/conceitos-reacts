yarn webpack-dev-server --mode development

css-loader -> vai ler o arquivo css e importações dentro css
style-loader-> vai pegar o arquivo 'lido' pelo css-loader e jogar no html


--configuração dentro do webpack -> https://github.com/rocketseat-education/bootcamp-gostack-apps/blame/master/02-conceitos-dev/frontend/webpack.config.js
const path = require('path');

module.exports = {
  entry: path.resolve(__dirname, 'src', 'index.js'),
  output: {
    path: path.resolve(__dirname, 'public'),
    filename: 'bundle.js'
  },
  devServer: {
    contentBase: path.resolve(__dirname, 'public'),
  },
  module: {
    rules: [
      {
        test: /\.js$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
        }
      },
      {
        test: /\.css$/,
        exclude: /node_modules/,
        use: [
          { loader: 'style-loader' },
          { loader: 'css-loader' },
        ]
      },
      {
        test: /.*\.(gif|png|jpe?g)$/i,
        use: {
          loader: 'file-loader',
        }
      }
    ]
  },
};